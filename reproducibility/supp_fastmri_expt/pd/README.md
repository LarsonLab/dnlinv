# Autocalibrated reconstruction experiments using 3T proton-density knee data from the fastMRI knee challenge

Run `doit.sh` in the command-line or run through the separate shell scripts sequentially.
